let exitButton: UIButton = {
        let eb = UIButton()
        eb.frame = CGRect(x: 50, y: 50, width: 100, height: 100)
        eb.setImage(UIImage(named: "home"), forState: .Normal)
        eb.alpha = 0.5
        return eb
    }()
